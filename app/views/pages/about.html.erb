<h1><%= @page.title %></h1>

<div>
  <%= simple_format(@page.content) %>
</div>

<% if user_signed_in? && current_user.admin? %>
  <%= link_to "✏️ Edit Page", edit_page_path(@page.slug), class: "btn btn-warning", style: "margin-top: 20px;" %>
<% end %>
